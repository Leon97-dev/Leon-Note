# 배열 메소드 분류표

✳️ 비파괴적 = 원본 그대로 두고 새로운 배열 반환
✳️ 파괴적 = 원본 배열 직접 수정

✅ 비파괴적 (Immutable)
원본 변화 없고, 새 배열을 반환한다.

- slice()
- filter()
- map()
- concat()
- flat()
- reduce()
- toSorted()
- toReversed()

⚠️ 파괴적 (Mutating)
원본 변화 있고, 원본 자체 변경된다.

- splice()
- pop()
- push()
- shift()
- unshift()
- reverse()
- sort()
- fill()

🧩 복사 계열
원본 변화 없고, 복사 후 새 배열 생성한다.

- spread(...)
- Array.from()
- structuredClone()
