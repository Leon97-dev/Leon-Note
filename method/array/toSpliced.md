# 배열 메서드 - toSpliced

_구분_
✂️ 비파괴적 삭제 / 삽입 / 교체

_실무 비율_
⭐⭐⭐

_설명_
기존 splice()는 원본 배열을 직접 변경하는 파괴형 메서드다.
하지만 toSpliced()는 원본을 유지하면서, 요소를 삭제, 삽입, 교체하는 새로운 배열을 만들어준다.

```js
arr.toSpliced(i, n, ...items) ===
  [...arr.slice(0, i), ...items, ...arr.slice(i + n)];
```

---

# 기본 문법

```js
array.toSpliced(start, deleteCount, item1, item2, ...);
```

반환값: 수정된 새 배열
원본 변경: ❌ (절대 안 바뀜)

---

# 기본 예시 1 — 삭제

```js
const arr = [1, 2, 3, 4];

const newArr = arr.toSpliced(1, 2);

console.log(newArr); // [1, 4]
console.log(arr); // [1, 2, 3, 4] (원본 유지)
```

💡 splice와 동일한 동작이지만, 원본이 안전하다.

---

# 기본 예시 2 — 삽입

```js
const arr = [1, 4];

const newArr = arr.toSpliced(1, 0, 2, 3);

console.log(newArr); // [1, 2, 3, 4]
```

---

# 기본 예시 3 — 교체

```js
const arr = ['레온', '민재', '지민'];

const replaced = arr.toSpliced(1, 1, '루시');

console.log(replaced); // ["레온", "루시", "지민"]
console.log(arr); // ["레온", "민재", "지민"]
```

💡 splice와 완전히 동일하지만 안전함.

---

# 음수 인덱스도 가능

```js
const arr = [1, 2, 3, 4];

const newArr = arr.toSpliced(-1, 1); // 마지막 요소 삭제

console.log(newArr); // [1, 2, 3]
```

---

# 실무 예시 1 — React에서 안전하게 배열 수정

```jsx
const newItems = items.toSpliced(index, 1);
setItems(newItems);
```

💡 splice는 절대 금지지만, toSpliced는 강력 추천.

---

# 실무 예시 2 - Prisma/DB 응답 데이터 수정

```js
const cleaned = results.toSpliced(0, 1); // 첫 번째 요소만 제거
```

💡 원본 보존이 중요한 데이터 처리에서 안정적.

---

# 실무 예시 3 - Array 교체 로직 간결화

```js
const list = [10, 20, 30];

const updated = list.toSpliced(1, 1, 99);

console.log(updated); // [10, 99, 30]
```

---

# 실무 예시 4 - 여러 개 삽입

```js
const arr = [1, 5];

const filled = arr.toSpliced(1, 0, 2, 3, 4);

console.log(filled); // [1, 2, 3, 4, 5]
```

---

# 실무 예시 5 - slice 조합을 대체

```js
// 기존 불변성 유지 코드
const newArr = [...arr.slice(0, i), newValue, ...arr.slice(i + 1)];

// 이제 한 줄
const newArr = arr.toSpliced(i, 1, newValue);
```

💡 훨씬 읽기 좋고 의도가 명확하다.

---

# 추가 정보

- toSpliced는 “불변성 기반 코드”의 필수 요소가 될 가능성이 높다.
- splice를 완전히 대체할 수 있으며, 의도가 더 명확하고 안전하다.
- Node 20+, 브라우저 최신 버전에서 기본 지원.
- toSorted, toReversed, with() 같은 ES2023 라인업과 함께 쓰면 최상의 안정성.
- 복잡한 배열 수정 로직을 가독성 있게 줄여준다.
